var buscraper = require('buscraper');

// Testing
buscraper.getClass("20174", "CASCX102", function(err, result) {
    if (err) {
        console.log(err);
        return;
    }
    console.log(JSON.stringify(result));
    console.log();
});

buscraper.getClasses("20174", ["CASCS111", "CASCH102", "CASWR100"], function(err, result) {
    if (err) {
        console.log(err);
        return;
    }
    console.log(JSON.stringify(result));
});
